import 'package:flutter/material.dart';

/// Function to return the custom child element for each item
/// in the list
///
/// @returns [Widget]
typedef ChildWidget = Widget Function(dynamic element);
